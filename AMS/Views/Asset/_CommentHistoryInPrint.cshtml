@model AMS.Models.AssetViewModel.AssetCRUDViewModel

<table class="CustomBlueTable" id="tblCommentHistoryInPrint" style="width:100%;" border="1">
    <thead>
        <tr>
            <th>Comentario</th>
            <th>Comentario por</th>
            <th>Fecha de comentario</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.listCommentCRUDViewModel)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Message)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CreatedBy)
                </td>
                <td>
                    @String.Format("{0:f}", item.CreatedDate)
                </td>
            </tr>
        }
    </tbody>
</table>

<script>
    $(document).ready(function () {
        $("#tblCommentHistoryInPrint").DataTable({
        });
    });
</script>